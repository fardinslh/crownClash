{
  "name": "crown-clash",
  "version": "0.1.0",
  "private": true,
  "description": "Crown Clash - Casual competitive territory-control game for messenger mini-apps",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "npm --workspace=apps/game run dev",
    "dev:server": "npm --workspace=apps/server run dev",
    "db:up": "docker compose up -d db",
    "db:down": "docker compose down",
    "build": "npm --workspace=packages/game-core run build && npm --workspace=packages/platform run build && npm --workspace=apps/game run build && npm --workspace=apps/server run build",
    "test": "npm --workspace=packages/game-core run test && npm --workspace=packages/platform run test && npm --workspace=apps/game run test",
    "test:server": "npm --workspace=apps/server run test",
    "typecheck": "npm --workspace=packages/game-core run typecheck && npm --workspace=packages/platform run typecheck && npm --workspace=apps/game run typecheck && npm --workspace=apps/server run typecheck"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
