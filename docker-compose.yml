services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: crownclash
      POSTGRES_PASSWORD: crownclash
      POSTGRES_DB: crownclash
    ports:
      - '5433:5432'
    volumes:
      - crownclash_pg_data:/var/lib/postgresql/data

volumes:
  crownclash_pg_data:
